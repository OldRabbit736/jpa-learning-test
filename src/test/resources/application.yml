spring:
  jpa:
    #    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:mysql://localhost:3311/jpa-learning-test
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
logging:
  level:
    org.hibernate:
      SQL: DEBUG
#        type:
#          descriptor:
#            sql:
#              BasicBinder: TRACE
